pipeline {
         agent any
         stages {
                 stage('build stage') {
                 steps {
                     sh 'cd order-service && mvn clean install'
                     echo 'Building Spring Boot application '
                 }
                 }
                 stage('build docker image stage ') {
                 steps {
                    sh 'cd order-service && docker build -t order-service .'
                    echo 'Building docker image'
                 }
                 }
         }
}
